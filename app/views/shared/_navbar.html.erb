<nav class="navbar navbar-expand-sm border-bottom py-2 fixed-top bg-primary" data-bs-theme="dark">
  <div class="container-fluid">

    <!-- LOGO + APP NAME -->
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center gap-2" do %>

      <!-- REPLACED IMAGE LOGO WITH CUSTOM EMOJI/RED BACKGROUND DIV -->
      <div class="logo-emoji me-1" style="width: 35px; height: 35px; font-size: 18px;">
        🧑🏽‍🍳
      </div>

      <!-- Applying Alegreya font via inline style -->
      <span class="fw-bold text-white" style="font-family: 'Alegreya', 'serif';">KitchenGuru</span>
    <% end %>

    <!-- MOBILE TOGGLER -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
            aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- NAVIGATION ITEMS -->
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto">

        <% if user_signed_in? %>

          <!-- MY RECIPES -->
          <li class="nav-item">
            <%= link_to "My Recipes", recipes_path, class: "nav-link fw-semibold text-white" %>
          </li>

          <!-- DROPDOWN MENU (Now with Profile Emoji) -->
          <li class="nav-item dropdown ms-2">
            <a href="#" class="nav-link p-0" id="navbarDropdown" data-bs-toggle="dropdown"
               aria-haspopup="true" aria-expanded="false" style="font-size: 28px; line-height: 1;">
              👨🏽‍🍳
            </a>

            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to "Profile", "#", class: "dropdown-item disabled text-dark" %>
              <div class="dropdown-divider"></div>
              <%= link_to "Log out",
                          destroy_user_session_path,
                          data: { turbo_method: :delete },
                          class: "dropdown-item text-danger" %>
            </div>
          </li>

        <% else %>

          <!-- LOGIN FOR NON LOGGED USER -->
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link fw-semibold text-white" %>
          </li>

          <!-- SIGNUP BUTTON -->
          <li class="nav-item ms-2">
            <%= link_to "Sign Up", new_user_registration_path,
                        class: "btn btn-outline-white btn-sm px-3 py-1" %>
          </li>

        <% end %>

      </ul>
    </div>
  </div>
</nav>
