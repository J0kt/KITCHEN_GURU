<div class="container py-5" style="max-width: 720px;">

  <!-- Back + delete actions -->
  <div class="d-flex justify-content-between align-items-center mb-4">
    <%= link_to "← Back to my recipes",
                recipes_path,
                class: "btn btn-outline-secondary btn-sm rounded-pill" %>

    <%= button_to "Delete this recipe",
                  recipe_path(@recipe),
                  method: :delete,
                  data: { confirm: "Remove this recipe from your cookbook?" },
                  class: "btn btn-outline-danger btn-sm rounded-pill" %>
  </div>

  <!-- Recipe card -->
  <div class="card shadow-sm border-0 rounded-4">
    <div class="card-body p-4">

      <h1 class="h3 fw-bold mb-2"><%= @recipe.title %></h1>
      <p class="text-muted mb-4"><%= @recipe.description %></p>

      <div class="d-flex gap-4 mb-4">
        <div class="small text-secondary">
          ⏱ <span class="fw-semibold"><%= @recipe.prep_time %> min</span>
          <span class="ms-1">prep</span>
        </div>
        <div class="small text-secondary">
          🔥 <span class="fw-semibold"><%= @recipe.cook_time %> min</span>
          <span class="ms-1">cook</span>
        </div>
      </div>

      <hr>

      <!-- Placeholder sections you can enrich later -->
      <div class="mb-3">
        <h2 class="h6 fw-semibold mb-2">Ingredients</h2>
        <p class="text-muted small mb-0">
          Coming soon – we’ll display structured ingredients here.
        </p>
      </div>

      <div>
        <h2 class="h6 fw-semibold mb-2">Steps</h2>
        <p class="text-muted small mb-0">
          Coming soon – we’ll show step-by-step instructions here.
        </p>
      </div>

    </div>
  </div>
</div>
